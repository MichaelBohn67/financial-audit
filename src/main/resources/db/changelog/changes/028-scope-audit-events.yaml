databaseChangeLog:
  - changeSet:
      id: 028-scope-audit-events
      author: financial-audit
      changes:
        - addColumn:
            tableName: audit_events
            columns:
              - column: { name: tenant_id, type: varchar(100) }
              - column: { name: project_id, type: varchar(100) }
